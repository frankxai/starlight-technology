# Hardware research receipt — 17 September 2026

Scope: one existing research reservation, `hw-502e439896c9d0098b72`. No new article, public URL, source feed, paid tool, affiliate destination or image was created.

## Result

The One UI 9 product research brief is saved at `data/editorial/packets/samsung-one-ui-9-20260917.json`. It separates manufacturer claims from local availability, records the Netherlands verification boundary, specifies useful visual assignments and declines an unjustified buying CTA. It is HOLD, not editorially approved or published.

The blocking reporting assignment is an exact regional model/build eligibility record and a clean, permission-safe workflow capture. Newsroom access alone does not clear image reuse. Independent review, Content OS overlap reconciliation and publication authority remain downstream gates.

## Durable state

The predecessor is `data/editorial/runs/2026-09-16-pilot.json` at `42cf7d1228d036676a03ad5ced4827370a1e2969`. PR #20 remained open and unrejected when inspected; no superseding hardware research PR was found. Its implementation branch is still a proposal, not merged policy.

Resume from `data/editorial/runs/2026-09-17-one-ui-research.json` in this research branch, including before merge. Its `nextState` preserves all three reservation IDs: two remain reserved; the researched item is held with its packet path and reopening condition. The initial intake is unchanged. Do not bootstrap again or count this held brief as a published article. Recheck this research branch and the implementation parent for concurrent updates before the next write.

## Evidence and cost

- Three unique manufacturer pages opened: global announcement, Dutch One UI page and newsroom terms. No failed source requests. No source binaries or full pages stored.
- Starlight content definitions, both complete repository trees and both open-PR lists inspected. Filename/title matching is not a complete semantic Content OS check; no new canonical URL assigned.
- The FrankX AI-tool affiliate catalog was inspected only as a routing reference. No approved hardware partnership asserted; no merchant price presented.
- Existing brief reservation: 6,000 input plus 2,000 output tokens. Additional reservation: zero. Monthly total remains 24,000 of 240,000.
- Planner model/API calls: zero. Actual research/model tokens and cost: unavailable from provider, not zero. No paid API/image-generation calls or human work claimed.

## Verification

- 11 planner tests pass.
- 13 existing application tests pass.
- Lint, typecheck and production build pass; 38 static pages generated.
- Packet claim/source references, HOLD/publication state, reservation preservation and duplicate replay assertions pass. Replay selects zero new jobs and leaves two pending.
- Dependency lockfile unchanged. Package installation used `--frozen-lockfile --ignore-scripts`.
- No UI or public article changes: visual preview checks are not applicable to this data-only diff. Independent review remains pending.

Rollback: retain the historical run bundle; supersede the held decision with a dated evidence-backed state transition. Reverting this research commit does not revoke the original reservations or imply permission to spend them twice.
